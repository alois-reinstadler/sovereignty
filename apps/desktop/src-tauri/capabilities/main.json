{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "main",
	"description": "The local vault window may acknowledge a pending close and select a bounded encrypted backup through an OS dialog. No general filesystem, network or key access.",
	"windows": ["main"],
	"permissions": ["allow-complete-close", "allow-encrypted-backup"]
}
