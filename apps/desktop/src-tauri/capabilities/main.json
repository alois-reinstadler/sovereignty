{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "main",
	"description": "The local vault window may acknowledge an already requested native close after locking. No storage, network or key access.",
	"windows": ["main"],
	"permissions": ["allow-complete-close"]
}
